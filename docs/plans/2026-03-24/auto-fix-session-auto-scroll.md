# Auto-Fix: Restore auto-scroll on new messages in SessionView

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Fix broken auto-scroll when new messages appear in session view.

**Root Cause:** In commit fb28003, the `.onChange(of: items.count)` handler's `else` branch (which auto-scrolled on new messages) was removed when the scroll-to-bottom button was added. Now it only scrolls on initial load, not when new messages arrive.

---

## Changes

### 1. Restore auto-scroll in SessionView.swift

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/SessionView.swift`

**Steps:**
1. Add `else if isNearBottom` branch to `.onChange(of: items.count)` to scroll to bottom when new items arrive and user is near bottom
2. Commit: `fix: restore auto-scroll on new messages in session view`

---

## Verification

- [ ] iOS unit tests pass
- [ ] iOS build succeeds
