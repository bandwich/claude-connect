# Auto-Fix: Filter system-injected messages from session titles

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Sessions with `<local-command-caveat>` as title appear in session list. These are system-injected messages, not user input.

**Root Cause:** `_parse_session_file()` picks the first `role: "user"` message as the title, but Claude Code injects system messages with `role: "user"` (like `<local-command-caveat>`) that aren't actual user input. The title extraction doesn't skip these, and `list_sessions()` doesn't filter them out.

---

## Changes

### 1. Skip system messages in title extraction

**Files:**
- Modify: `voice_server/session_manager.py`
- Test: `voice_server/tests/test_session_manager.py`

**Steps:**
1. Write failing test: session with `<local-command-caveat>` first message should get title from next real user message
2. Write failing test: session with ONLY `<local-command-caveat>` messages should be filtered out
3. Add system message detection to `_parse_session_file()` - skip messages starting with `<local-command-caveat>`, `<task-notification`, `Base directory for this skill:`
4. Run tests, verify passes
5. Commit

## Verification

- [ ] `cd voice_server/tests && ./run_tests.sh`
