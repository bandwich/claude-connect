# Auto-Fix: iOS Voice Recording Issues

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Fix three iOS voice mode issues: (1) cannot stop recording, (2) recording persists on navigation, (3) title truncation on partial swipe

**Root Cause / Approach:**
1. The mic button is disabled when `voiceState != .idle`, but recording sets `voiceState = .listening`, making it impossible to tap the button to stop. The button should remain enabled during recording.
2. No cleanup when navigating away from SessionView - need `.onDisappear` to stop recording.
3. The title in `LeadingNavContent` lacks a width constraint, causing it to expand during swipe gestures.

---

## Changes

### 1. Fix mic button disabled during recording

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/SessionView.swift`
- Test: `ios-voice-app/ClaudeVoice/ClaudeVoiceTests/ClaudeVoiceTests.swift` (add test)

**Steps:**
1. Write failing test that verifies mic button remains enabled while recording
2. Run test, verify fails
3. Fix: Split `canRecord` into `canStartRecording` and separate button disable logic so the button stays enabled while recording (for stopping)
4. Run test, verify passes
5. Commit: `fix: allow stopping voice recording by keeping mic button enabled`

### 2. Fix recording not stopped on navigation back

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/SessionView.swift`

**Steps:**
1. Write failing test that verifies recording stops when view disappears
2. Run test, verify fails
3. Fix: Add `.onDisappear` handler to stop recording and clean up audio player
4. Run test, verify passes
5. Commit: `fix: stop recording when navigating away from SessionView`

### 3. Fix title truncation on partial swipe

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/CustomNavigationBar.swift`

**Steps:**
1. Fix: Add explicit width constraint to title VStack in `LeadingNavContent` using `.frame(maxWidth:)` or `.fixedSize(horizontal: false, vertical: true)`
2. Verify manually (visual layout issue, hard to unit test)
3. Commit: `fix: ensure session title truncates during swipe gestures`

---

## Why E2E Tests Pass

The E2E tests use `sendVoiceInput()` which sends voice input directly via WebSocket, completely bypassing the mic button tap and SpeechRecognizer flow. They never test:
- Tapping the mic button to start recording
- Tapping again to stop recording
- The button's enabled/disabled state during recording

This is a testing gap - the E2E tests don't cover the actual voice recording UI interaction.

---

## Verification

Run all project tests:
- [ ] Server tests: `cd voice_server/tests && ./run_tests.sh`
- [ ] iOS unit tests: `xcodebuild test -scheme ClaudeVoice -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ClaudeVoiceTests`
- [ ] iOS E2E tests: `cd ios-voice-app/ClaudeVoice && ./run_e2e_tests.sh`
