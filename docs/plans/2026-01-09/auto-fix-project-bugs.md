# Auto-Fix: Project Path Display and Spaces Bugs

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Fix two bugs: (1) project folder paths display incorrectly (underscores converted to slashes), (2) project creation doesn't preserve spaces.

**Root Cause / Approach:**
1. Path display: `list_projects()` naively replaces all `-` with `/`, but Claude encodes BOTH `/` and `_` as `-`. Fix by reading `cwd` from newest session file to get actual path.
2. Spaces bug: `handle_add_project()` filters `safe_name` with `c.isalnum() or c in "-_."` which excludes spaces. Fix by adding space to allowed characters.

---

## Changes

### 1. Fix path display in list_projects()

**Files:**
- Modify: `voice_server/session_manager.py`
- Test: `voice_server/tests/test_session_manager.py`

**Steps:**
1. Write failing test that checks folder path decodes correctly (verifying underscore is not converted to slash)
2. Run test, verify fails
3. Modify `list_projects()` to read `cwd` from newest session file; fall back to decoded path if no sessions exist
4. Run test, verify passes
5. Commit: `fix: decode project folder paths correctly using session cwd`

### 2. Fix project creation spaces bug

**Files:**
- Modify: `voice_server/ios_server.py`
- Test: `voice_server/tests/test_message_handlers.py`

**Steps:**
1. Write failing test for project creation preserving spaces in name
2. Run test, verify fails
3. Add space to allowed characters in `safe_name` filter
4. Run test, verify passes
5. Commit: `fix: preserve spaces in project names`

---

## Verification

Run all project tests:
- [ ] `cd voice_server/tests && ./run_tests.sh`
- [ ] iOS unit tests (if path affects iOS models)
