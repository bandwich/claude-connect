# Auto-Fix: Filter synthetic messages from session history

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** "No response requested" synthetic messages appear in iOS app when viewing session history.

**Root Cause:** `session_manager.py:get_session_history()` doesn't check for `model == "<synthetic>"` on assistant messages. The live transcript watcher (`ios_server.py:272`) already filters these, but the history loading path does not.

---

## Changes

### 1. Filter synthetic messages in session_manager.py

**Files:**
- Modify: `voice_server/session_manager.py`
- Test: `voice_server/tests/test_session_manager.py`

**Steps:**
1. Write failing test: session history with a synthetic message should exclude it
2. Run test, verify fails
3. Add `model == '<synthetic>'` check for assistant messages in `get_session_history()`
4. Run test, verify passes
5. Commit: `fix: filter synthetic assistant messages from session history`

---

## Verification

- [ ] `cd voice_server/tests && ./run_tests.sh`
