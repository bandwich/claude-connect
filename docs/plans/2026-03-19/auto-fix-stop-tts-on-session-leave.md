# Auto-Fix: Stop TTS when leaving a session

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** When leaving a session (close, stop, or switch to another), cancel any in-progress or queued TTS for that session.

**Approach:** Extract TTS cancellation into a helper method `cancel_tts()`, then call it from `handle_close_session`, `handle_stop_session`, and `handle_view_session`.

---

## Changes

### 1. Add `cancel_tts()` method and call it on session leave

**Files:**
- Modify: `voice_server/ios_server.py`
- Test: `voice_server/tests/test_message_handlers.py`

**Steps:**
1. Write failing tests: close_session cancels TTS, stop_session cancels TTS, view_session cancels TTS
2. Run tests, verify they fail
3. Add `cancel_tts()` method that sets cancel event, drains queue, sends stop_audio
4. Call it from handle_close_session, handle_stop_session, handle_view_session
5. Run tests, verify they pass
6. Commit: `fix: cancel TTS when leaving a session`

---

## Verification

- [ ] `cd voice_server/tests && ./run_tests.sh`
