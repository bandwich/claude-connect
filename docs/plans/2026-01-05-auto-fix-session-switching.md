# Auto-Fix: Fix Session Switching Navigation

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Fix delayed session switching - selecting a new Claude session doesn't work until navigating back

**Root Cause:** `SessionsListView` uses `navigationDestination(isPresented:)` with a separate `selectedSession` state, causing timing issues. When tapping a new session, SwiftUI may evaluate the navigation destination before state updates are fully applied, or may not properly trigger `onAppear` for the new session when navigation state is already "active". This results in the session switch action being delayed until the next navigation event.

---

## Changes

### 1. Session Model - Add Hashable Conformance

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Models/Session.swift`

**Steps:**
1. Add `Hashable` conformance to `Session` struct (required for `navigationDestination(item:)`)
2. Commit: `fix: add Hashable to Session for navigation`

### 2. SessionsListView - Fix Navigation Pattern

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/SessionsListView.swift`

**Steps:**
1. Remove `@State private var showingSessionView = false` (no longer needed)
2. Change `navigationDestination(isPresented:)` to `navigationDestination(item:)` bound directly to `$selectedSession`
3. Update button action to only set `selectedSession = session` (navigation happens automatically)
4. For new session creation, set `selectedSession` after successful action
5. Commit: `fix: use navigationDestination(item:) for proper session switching`

---

## Verification

Run all project tests:
- [ ] `cd voice_server/tests && ./run_tests.sh`
- [ ] `cd ios-voice-app/ClaudeVoice && xcodebuild test -scheme ClaudeVoice -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ClaudeVoiceTests`
