# Auto-Fix: Restore auto-scroll and fix activity indicator timing

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Fix broken auto-scroll when new messages appear, and fix thinking indicator flashing momentarily after messages.

**Root Causes:**
1. In commit fb28003, the `.onChange(of: items.count)` handler's `else` branch (which auto-scrolled on new messages) was removed when the scroll-to-bottom button was added.
2. Activity state (thinking/idle) was only updated via 1s pane polling, so the "idle" update lagged behind the message by up to 1 second, causing a brief thinking flash after messages appeared.

---

## Changes

### 1. Restore auto-scroll in SessionView.swift

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/SessionView.swift`

**What:**
- Add `else if isNearBottom` branch to `.onChange(of: items.count)` to scroll to bottom when new items arrive and user is near bottom
- Add `.onChange(of: webSocketManager.activityState?.state)` to scroll to bottom when activity changes (e.g., thinking starts)

### 2. Event-driven activity state re-check

**Files:**
- Modify: `voice_server/server.py`

**What:**
- Extract pane check logic from `_pane_poll_loop` into reusable `_check_activity_state()` method
- Call `_check_activity_state()` immediately after `handle_content_response` sends an assistant message
- This ensures the activity state update arrives right after the message, not up to 1s later

---

## Verification

- [x] iOS build succeeds
- [x] Manual testing confirms auto-scroll works
- [x] Manual testing confirms thinking flash is gone
