# Auto-Fix: Render markdown in iOS assistant messages

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Fix literal `**text**` asterisks showing in iOS app instead of bold formatting.

**Root Cause:** `MessageBubble` uses `Text(message.content)` which treats String variables as plain text in SwiftUI. Also, TTS receives raw markdown and may speak asterisks.

---

## Changes

### 1. iOS MessageBubble - render markdown

**Files:**
- Modify: `ios-voice-app/ClaudeVoice/ClaudeVoice/Views/SessionView.swift`

**Steps:**
1. In `MessageBubble`, change `Text(message.content)` for assistant messages to use `AttributedString(markdown:)` with `.inlineOnlyPreservingWhitespace` option
2. Fallback to plain text if markdown parsing fails

### 2. Server TTS - strip markdown before speaking

**Files:**
- Modify: `voice_server/ios_server.py`

**Steps:**
1. Add markdown stripping in `extract_text_for_tts` - remove `**`, `*`, `` ` ``, `#` prefixes, `[text](url)` → `text`
2. Add test for stripping

---

## Verification

- [ ] Server tests: `cd voice_server/tests && ./run_tests.sh`
- [ ] iOS build: `xcodebuild build -scheme ClaudeVoice -destination 'platform=iOS Simulator,name=iPhone 16'`
