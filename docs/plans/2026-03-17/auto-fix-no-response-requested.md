# Auto-Fix: Filter synthetic "No response requested" messages

> **For Claude:** This plan was generated by auto-fix skill. Execute autonomously.

**Task:** Prevent synthetic Claude Code messages (like "No response requested") from appearing as assistant bubbles in the iOS app.

**Root Cause:** Claude Code writes synthetic messages to the transcript JSONL with `"model": "<synthetic>"`. The TranscriptHandler extracts these as normal assistant content and broadcasts them to iOS. They should be filtered out.

---

## Changes

### 1. Filter synthetic messages in TranscriptHandler

**Files:**
- Modify: `voice_server/ios_server.py`
- Test: `voice_server/tests/test_content_handler.py`

**Steps:**
1. Write failing test: synthetic assistant message with `model: "<synthetic>"` should produce no content blocks
2. Run test, verify fails
3. Add check in `extract_new_content()` to skip assistant messages where `msg.get('model') == '<synthetic>'`
4. Run test, verify passes
5. Commit: `fix: filter synthetic assistant messages from transcript`

---

## Verification

- [ ] Server tests: `cd voice_server/tests && ./run_tests.sh`
